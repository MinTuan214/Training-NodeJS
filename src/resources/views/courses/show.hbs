<div class="mt-4">
    <div class="row">
        <div class="col-3">
            <button> Hoc ngay </button>
        </div>
        <div class="col-9">
            <h2>{{course.name}}</h2>
            <img src="{{course.image}}" alt="">
            <p>{{course.description}}</p>
        </div>
    </div>
</div>